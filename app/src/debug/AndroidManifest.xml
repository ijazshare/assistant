<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only additions. This whole source set is absent from release builds, so
  the diagnostics screen cannot ship to users by accident — it is not merely
  hidden behind a flag, it does not exist in the release APK.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <activity
            android:name=".ui.debug.DebugActivity"
            android:exported="true"
            android:label="Machine Debug"
            android:theme="@style/Theme.TheMachine">
            <!--
              Its own launcher entry: reaching this from adb alone would mean typing a
              component name every time, and it needs to be usable on the phone.
            -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
